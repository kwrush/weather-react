/**
 * Utilities to format response from api server
 */

import {Map, List} from 'immutable';

export const interpretWeatherData = (jsonData) => {
    
    return Map({
        
    });
}

export const interpretGeoLocation = (jsonData) => {
    return Map({

    });
}